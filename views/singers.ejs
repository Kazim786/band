<!DOCTYPE html>
<html>
    <head>
    <title><%=pageTitle %></title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
    
    <link rel='stylesheet' href='/css/style.css' />
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans|Roboto+Slab' rel='stylesheet' type='text/css'>
    
    </head>
    <body id="<%= pageID %>">
    <header>
        <div class="jumbotron hidden-xs">
        </div><!-- jumbotron -->
        
        <nav class="navbar navbar-inverse">
            <div class="container">
            <div class="navbar-header">
                
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                </button>
                
                <a href="/" class="navbar-brand">Creative Artist Meetups</a>
            </div>
            <div class="collapse navbar-collapse" id="navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                <li><a href="/">Home</a></li>
                    <li><a href="/singers" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Singers <span class="caret"><span></a>
                    <ul class="dropdown-menu">
                        <li><a href="/singers">All Singers</a></li>
                        <li role="separator" class="divider"></li>

                        
                        <% allsingers.forEach(function(item) { %>
                        <li><a href="/singers/"><%= item.name %></a></li>
                        <% }); %>
                    </ul>
                </li>
                <li><a href="/feedback">Feedback</a></li>
                <li><a href="/chat">Chat</a></li>
                </ul><!-- nav -->
            </div><!-- navbar-collapse -->
            </div><!-- container -->
        </nav>
        </header>
        
    <div class="container">
        <div class="row">
        <div class="col-sm-8">
            <article class="singerslist <% if (pageID == 'home') { %>jumbotron hidden-xs<% } %>">
                <div class="container">
                    <div class="row">
                        <% allsingers.forEach(function(item) { %>
                        <div class="<% if (pageID == 'home') { %>col-sm-4 text-center<% } else { %>col-sm-8 <% } %>">
                        <h3 class="singerslist-title"><%= item.title %></h3>
                        <h5 class="singerslist-name">with <a href="/singers/"><%= item.name %></a></h5>
                        <p class="singerslist-info">
                            <a href="/singers/">
              
                            <% if (pageID == 'home') { %>
                              <img class="singerslist-img img-circle" src="/images/singers/" alt="Photo of <%= item.name %>"></a>
                            <% } %>
              
                            <% if (pageID == 'singerList') { %>
                              <img class="singerslist-img img-circle pull-left" src="/images/singers/" alt="Photo of <%= item.name %>"></a>
                            <% } %>
                          </a>
              
                          <% if (pageID == 'singerDetail') { %>
                            <img class="singerslist-img img-responsive pull-left" src="/images/singers/" alt="Photo of <%= item.name %>"></a>
                          <% } %>
                        </a>
              
                          <% if (pageID !== 'home') { %>
                            <% if (pageID == 'singerList') { %>
                              <%= item.summary %>
                            <% } else { %>
                              <%- item.description %>
                            <% } %>
                          <% } %>
                        </p>
                      </div>
                      <%  }); %>
                  </div><!-- row -->
                </div><!-- container -->
              </article>
              
        </div><!-- primary -->
        <aside class="col-sm-4">
            <article class="sidebar">
                <h1 class="sidebar-title">Songs on display</h1>
                <p class="sidebar-body">While you wait for the concert to begin, head over to our music gallery where you can check out some of the work from our singers.</p>
                <div class="pixgrid clearfix">
                  
    
                </div>
              </article>
              
        </aside><!-- secondary -->
      </div>
    </div><!-- container -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js" integrity="sha384-0mSbJDEHialfmuBBQP6A4Qrprq5OVfW37PRR3j5ELqxss1yVqOtnepnHVP9aJ7xS" crossorigin="anonymous"></script>

    <% if(typeof artwork !== "undefined") { %>
    <script src="/js/pixgrid.js"></script>
    <% } %>

    <% if(pageID == 'feedback') { %>
    <script src="/js/feedback.js"></script>
    <% } %>

    <!-- <script src="https://cdn.socket.io/socket.io-1.4.5.js"></script>
    <script src="/js/chat.js"></script>
    <script src="/reload/reload.js"></script> -->
  </body>
</html>